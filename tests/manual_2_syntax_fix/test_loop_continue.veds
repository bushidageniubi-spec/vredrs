# Test loop with continue
set, i = 0
set, sum = 0
loop
    set, i = i + 1
    if, i > 10
        break
    /end
    if, i % 2 == 0
        continue
    /end
    set, sum = sum + i
/end
println, sum
# Expected: 1+3+5+7+9 = 25
